# ── Servidor backend ──────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9   # necesario para UploadFile en FastAPI

# ── Modelo de lenguaje ─────────────────────────
torch>=2.1.0
transformers>=4.43.0      # Qwen2.5 requiere >= 4.43
accelerate>=0.30.0        # device_map="auto"
sentencepiece>=0.1.99     # tokenizador

# ── Extracción de PDF ──────────────────────────
pymupdf>=1.24.0           # fitz — texto ordenado por bloques
pdfplumber>=0.11.0        # extracción de tablas

# ── Frontend ───────────────────────────────────
gradio>=4.36.0
requests>=2.31.0

# ── Opcional: OCR para PDFs escaneados ─────────
# pytesseract>=0.3.10
# Pillow>=10.0.0